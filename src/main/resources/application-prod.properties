# Producción - conexión real a base de datos
spring.datasource.url=jdbc:mysql://10.0.1.50:3306/smartfix
spring.datasource.username=smartfix_admin
spring.datasource.password=${DB_PASSWORD}

# Driver explícito
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Seguridad
server.ssl.enabled=true
server.port=8443

# Logging mínimo
logging.level.root=ERROR
logging.level.org.springframework.web=ERROR

# Actuator y monitoreo
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

# CORS y otras configuraciones personalizadas si usas controladores REST
# smartfix.api.allowed-origins=https://midominio.com
